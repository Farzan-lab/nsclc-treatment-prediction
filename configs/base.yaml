# Base configuration — shared across all experiments

data:
  processed_path: "data/processed/nsclc_final.csv"
  splits_dir: "data/splits/"
  n_folds: 5
  test_size: 0.15
  random_state: 42

features:
  clinical_dim: 16
  genomic_dim: 50

training:
  random_state: 42
  n_jobs: -1

logging:
  experiment_dir: "experiments/"
  figures_dir: "figures/"
